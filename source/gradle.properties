org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official


# AGP 9.x currently rejects LiteRT 2.2.0 because litert and its required litert-api AARs declare the same namespace.
# This is a temporary upstream-tracked LiteRT packaging workaround; do not exclude litert-api because it is required at runtime.
android.uniquePackageNames=false
